# Level 8.3 — Core vs Detail (protocol registered, not yet a result)

Each example contains a persistent core state and a concrete episodic detail, followed by separate Core and Detail queries. No route label is imposed. Analyze whether Slow Memory preferentially supports Core queries and Episodic top-k retrieval preferentially supports Detail queries.

Required causal panels: normal, zero Slow, zero Episodic, keep-only Fast, keep-only Slow, keep-only Episodic, sample swap, and slot roll. This directory currently registers scope only; it contains no formal result.
