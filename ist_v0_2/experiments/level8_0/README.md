# Level 8.0 — Hierarchical Memory Smoke Test

Checks forward/backward, exact state shapes, cross-Chunk state change, atomic checkpoint recovery, all module-disable ablations, all zero/freeze/keep-only/roll/swap interventions, strict loading of a real v0.1 checkpoint, and v0.1→v0.2 shared/Fast weight transfer.

Formal artifacts include `config.json`, `checkpoint.pt`, `raw_results.json`, `result.json`, `ANALYSIS.md`, and `run_metadata.json`.
