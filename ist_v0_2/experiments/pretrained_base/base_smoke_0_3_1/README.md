# Base Smoke 0.3.1 — Fast-Only Confirmation

Router, Slow, Episodic, and consolidation are disabled. The FP32 residual gate drives only the persistence delta from Fast Memory. `zero_fast` now removes both Fast read and the state-dependent Fast base feature and must equal reset.

```powershell
python run_pretrained_base_smoke_0_3_1.py --dry-run
python run_pretrained_base_smoke_0_3_1.py --local-files-only
```
