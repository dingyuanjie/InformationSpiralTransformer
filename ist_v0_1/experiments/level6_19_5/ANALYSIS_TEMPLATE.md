# Level 6.19.5 formal analysis

## Decision

- integrity:
- registered bottleneck classification:
- frozen-router recovery:
- Oracle-dose + learned-direction recovery:
- learned-dose + Oracle-direction recovery:
- signed versus residual basis conclusion:
- registered next boundary:

## Integrity

Report frozen fingerprints, frozen `requires_grad`, parent-router fingerprints,
equal direction-probe parameter counts, matched trainable initialization,
Oracle L2 error, disjoint split seeds,
fixed-final-epoch use, validation non-selection, formal diagnostic isolation,
failed-candidate exclusion, seed909 lock, protected-test lock, and closed
optimizer/model search.

## Frozen dose-direction hybrids

Report primary margin gain, full-Oracle recovery, 25% threshold status, primary
correction rate, and full-panel paired accuracy change for every hybrid. Label
Oracle arms explicitly as non-deployable diagnostics.

## Held-out observability

Report primary-classifier AUROC, AUPRC, prevalence, fixed-prevalence precision
and lift. Report dose R2, Spearman, MAE/RMSE, and calibration. Report signed and
residual direction cosine, first-order alignment, positive-alignment fraction,
and deployed recovery at Oracle dose.

## Scientific conclusion

Apply only the pre-registered four-way bottleneck rule. Keep the observability
ceilings distinct from the frozen Level 6.19.4 router. Do not claim that any
label-aware hybrid is deployable.

## Next experiment

Follow only `analysis.diagnosis.registered_next_boundary` in `result.json`.
