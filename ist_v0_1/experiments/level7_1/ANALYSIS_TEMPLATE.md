# Level 7.1 formal analysis

## Decision

- integrity:
- seed1217 formation / causal gate:
- seed1429 formation / causal gate:
- registered primary classification:
- secondary read-gap replication:
- registered stop boundary:

## Independent formation

Report each curriculum stage, final fresh 16-chunk query/local/probe metrics,
runtime, and whether training stopped without extension.

## Causal replication

For each formed initialization, report intact, reset-all, zero-all,
batch-roll-all, zero-L3, batch-roll-L3, and keep-L3 query/local accuracy on the
paired fresh causal split. Apply the registered thresholds exactly.

## Frozen read-gap diagnostic

Report split separation, probe validation accuracy, full test accuracy, source
error count, final-layer-Memory probe accuracy on source errors, query-hidden
probe accuracy on source errors, and their difference. State that these probes
were never used to change or gate the model.

## Scientific conclusion

Separate unconditional formation reliability, causal Memory replication
conditional on formation, and the secondary read-gap generalization. Do not
replace a seed, extend a budget, perform output-head rescue, or reopen the
Level 6.19 router branch.
