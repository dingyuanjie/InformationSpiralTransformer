# Level 7.3 formal analysis

## Decision

- integrity:
- whole-Memory causal passes:
- distinct layer signatures:
- registered classification:
- stop boundary:

## Frozen sources

Report provenance, source behavior, checkpoint fingerprints, and confirmation
that no parameter changed.

## Shared causal atlas

Report all 16 conditions for all four models on the same fresh 2,048-example
panel. Separate whole-Memory causality from layer localization.

## Layer signatures

Report necessary, misassignment-sensitive, single-layer-sufficient, and
pair-sufficient sets under the registered thresholds. Apply the deterministic
profile classifier without changing thresholds.

## Scientific conclusion

State which persistent-Memory claims generalize across initialization and
which layer-localization claims do not. Do not select or repair a source model.
